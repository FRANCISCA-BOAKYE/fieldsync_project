{
  "name": "fieldsync-project",
  "private": true,
  "workspaces": [
    "frontendui",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontendui",
    "build": "npm run build --workspace frontendui"
  }
}
